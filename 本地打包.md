scripts/build-cli-docker.sh。它默认按当前机器识别 GOOS/GOARCH，所以构建出来的是本地可运行版本。

我已经跑过一次，产物在：

server/bin/multica

验证结果：

server/bin/multica: Mach-O 64-bit executable x86_64
go: go1.26.1, os/arch: darwin/amd64

使用方式：

./scripts/build-cli-docker.sh
./server/bin/multica version

安装到 PATH：

sudo install -m 755 server/bin/multica /usr/local/bin/multica
multica version

如果你要强制构建 Apple Silicon 版本：

TARGET_OS=darwin TARGET_ARCH=arm64 ./scripts/build-cli-docker.sh

另外我把 .cache/ 加进了 .gitignore，因为 Docker 构建会缓存 Go 依赖到本地。
# 本地打包
./scripts/build-cli-docker.sh
